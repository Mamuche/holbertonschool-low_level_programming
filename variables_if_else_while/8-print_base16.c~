#include <stdio.h>
/**
 * main - print numbers 0 to 16.
 *
 * Return: always 0.
 */

int main(void)
{
	int number;
	char letter;

	for (number = 0; number < 10; number++)
	{
		putchar(number);
	}
	for (letter = 'a'; letter <= 'f'; letter++)
	{
		putchar(letter);
	}

	putchar('\n');
	return (0);
}
