#include <stdio.h>
/**
 *main - print alphabet lowercase and uppercase
 *
 *Return: alway 0
 */

int main(void)
{
	char alphabet = (('a')('A'));
	while (alphabet <= ('z' 'Z'))
	{
		putchar(alphabet);
		alphabet++;
	}
		return (0);
}
