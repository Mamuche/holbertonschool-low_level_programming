#include "main.h"

/**
 * print_alphabet_10x - print alphabet 10x.
 */

void print_alphabet_10x(void)
{
	int num = 0;
	char letter;

	while (num++ <=9)
	{
		for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);
		_putchar('\n');
	}
}
